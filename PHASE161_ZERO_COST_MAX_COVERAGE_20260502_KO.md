# PHASE161 — 무료 최대 커버리지 진단 구조 최적화 완료

## 목표

최소 비용·최소 시간·최소 운영 노동 원칙을 유지하면서, 데모/서비스 진단이 실제로 믿을 수 있는 산출물을 내도록 구조를 정리했다.

외부 유료 API가 없어도 작동해야 하므로 기본 진단 원천은 Gemini나 외부 스캔 API가 아니라 서버 내장 규칙 엔진과 공개 페이지 수집으로 고정했다. Gemini, Lighthouse, Search Console, Playwright는 필요 시 켤 수 있는 보조 계층으로만 남겼다.

## 핵심 변경

1. 기본 스캔 제공자를 `external_http`에서 `builtin`으로 변경했다.
2. `NV0_TARGET_FETCH_ENABLED=true`를 기본 배포 예시값으로 변경했다.
3. 홈 1회 수집 후 실제 내부 링크를 분석해 개인정보, 약관, 환불, 문의, 사업자, 결제, 배송, 가격, FAQ 후보 링크를 우선 수집한다.
4. 고정 8개 경로만 보던 구조를 적응형 후보 수집 구조로 바꿨다.
5. 최대 수집 페이지 수, 동시성, 타임아웃을 환경변수로 제어한다.
6. 실패한 후보 페이지도 버리지 않고 결과에 남긴다.
7. 실패/차단/빈 페이지는 “없음”으로 단정하지 않고 수동확인 필요 항목으로 표시한다.
8. 사업자 정보 규칙이 단순히 `<footer>` 존재만으로 통과되던 문제를 제거했다.
9. 가격, 배송/제공 시점, 구독/자동결제, 입력폼 동의 근접성 규칙을 추가했다.
10. 결과 화면에서 수동 확인 필요 개수를 금액처럼 보이지 않게 수정했다.

## 기본 운영값

```env
NV0_SCAN_PROVIDER=builtin
NV0_TARGET_FETCH_ENABLED=true
NV0_TARGET_FETCH_MAX_PAGES=12
NV0_TARGET_FETCH_CONCURRENCY=4
NV0_TARGET_FETCH_TIMEOUT_MS=3000
```

위 설정은 외부 유료 API 없이 동작한다. 요청당 최대 12개 공개 페이지를 4개 동시성으로 수집하므로 속도와 커버리지의 균형을 맞춘 값이다.

## 진단 방식

```text
입력 URL
→ 홈/입력 경로 수집
→ HTML 내부 링크 추출
→ 정책·결제·문의·환불·가격·배송 후보 링크 점수화
→ 상위 후보만 병렬 수집
→ 성공/실패 페이지 모두 기록
→ 규칙 엔진으로 발견/미확인/수동확인 분리
→ 결과 화면에 근거·확인 URL·한계 표시
```

## 추가된 탐지 범위

- 사업자 정보 고지
- 개인정보처리방침
- 이용약관
- 환불·교환·청약철회
- 고객센터 연락수단
- 과장·확정형 표현
- HTTPS
- 쿠키/추적 고지
- 청소년 보호 주의 문구
- 결제 전 주요 고지
- 제공 범위
- 총 결제금액·추가비용
- 배송·제공 시점
- 구독·자동결제 조건
- 입력폼 주변 동의·개인정보 안내
- 법률 자문 아님 고지

## 한계 표시 원칙

자동 수집으로 볼 수 없는 영역은 감추지 않는다.

- 로그인 후 화면
- 외부 결제창
- JS 전용 렌더링 화면
- robots/방화벽/차단 페이지
- 실패한 후보 URL
- 법률 판단이 필요한 광고·업종 표현

이 영역은 결과에서 수동확인 필요로 안내한다.

## 검증 결과

- 문법 검사: 168개 통과
- 전체 테스트: 85개 통과 / 실패 0
- E2E: 통과
- 라우트 스모크: 24개 통과
- 링크 검사: 149개 통과 / 오류 0
- P156 전역 UX 검증: 64개 통과
- P157 비결제 운영 검증: 19개 통과
- P158 E2BIG 핫픽스 검증: 8개 통과
- P159 데모/게시판 검증: 18개 통과
- P160 근거 기반 진단 검증: 23개 통과
- P161 무료 최대 커버리지 검증: 21개 통과
- deploy precheck: 통과
- runtime clean release: 통과

## 결론

이번 버전은 외부 API 비용 없이도 공개 페이지 기준에서 가능한 탐지 누락을 최소화하는 구조다. 다만 로그인 후 화면, 외부 결제창, JS 렌더링 전용 화면, 법률 판단 영역은 기술적으로 자동 확정할 수 없으므로 수동확인 필요로 남긴다. 이는 과장 없이 신뢰 가능한 산출물을 만들기 위한 필수 구조다.
