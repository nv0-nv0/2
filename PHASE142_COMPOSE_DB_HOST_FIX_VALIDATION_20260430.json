{
  "ok": true,
  "phase": 142,
  "fix": "compose-internal-postgres-host",
  "rootCause": "NV0_DATABASE_URL pointed to a non-resolvable Coolify hostname instead of the compose service name postgres",
  "changes": [
    "docker-compose.yml sets NV0_DATABASE_URL internally to postgres://nv0:${POSTGRES_PASSWORD}@postgres:5432/nv0",
    "deploy/docker-compose.coolify.yml sets NV0_DATABASE_URL internally to postgres://nv0:${POSTGRES_PASSWORD}@postgres:5432/nv0",
    "POSTGRES_DB and POSTGRES_USER are fixed to nv0 in compose",
    "generate-r2-coolify-env no longer emits NV0_DATABASE_URL",
    "postgres password generator uses URL-safe base64url"
  ],
  "operatorAction": [
    "Remove stale NV0_DATABASE_URL from Coolify Environment Variables if present",
    "Keep POSTGRES_PASSWORD",
    "Reload Compose File",
    "Redeploy"
  ],
  "limitations": [
    "Docker compose runtime validation was not executed in this environment"
  ]
}
