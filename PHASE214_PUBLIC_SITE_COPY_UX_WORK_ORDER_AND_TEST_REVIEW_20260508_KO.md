# PHASE214 공개 사이트 문구·UX/UI 전면 개편 작업지시서 및 테스트 리뷰

## 1. 문제 진단

사용자가 지적한 `/plans` 화면은 구매 전환 페이지가 아니라 내부 검수 화면처럼 보였습니다. 특히 다음 문제가 컸습니다.

- 표 형태 비교 영역이 깨져 보이며 첫 열에 문장이 몰려 가독성이 떨어짐
- “결제 가능 상태 확인”, “받을 결과물과 금액 확인” 같은 보조 패널이 구매 욕구보다 불안을 키움
- 추천 카드와 요약 카드가 중복되어 시선이 흩어짐
- 내부 운영·검증·점수 기준 성격의 문구가 공개 화면에 남아 있음
- 사이트 전체에서 “무엇을 사야 하는지”보다 “시스템이 무엇을 하는지”가 먼저 보임

## 2. 개편 원칙

- 방문자가 느끼는 문제를 먼저 말한다.
- 기능보다 받을 결과물을 먼저 보여준다.
- 상품 선택은 표가 아니라 상황별 카드로 정리한다.
- 구매 전 불필요한 내부 설명은 공개 화면에서 제거한다.
- 결제 연동 구조는 유지하되, 문구는 구매자 관점으로 바꾼다.
- 모바일에서도 한 화면에 하나의 판단만 하게 만든다.

## 3. 적용 범위

### `/plans`

- 두 번째 스크린샷에 해당하는 집중도 저하 영역 제거
  - `payment-readiness-panel` 제거
  - `recommended-layout` 제거
- 비교표 제거
  - `<table class="comparison-table">` 제거
  - `plan-decision-grid` 카드형 선택 구조로 변경
- 히어로 문구 전면 개편
- 구매 고통 지점 3개 재작성
- 상품 흐름 4단계 재정리
- FixPack 추천 영역을 간결한 집중 카드로 교체
- 상품 카드 문구 재작성
- 상황별 선택 카드 추가
- Before/After 문장 예시 재작성
- FAQ와 최종 CTA 재작성

### `/checkout`

- “결제 전 검수” 느낌을 줄이고, 신청 후 받을 결과물 중심으로 수정
- 이메일 입력, 결과물 안내, 환불 제한 고지를 구매자 관점으로 재정리
- 결제 구조는 유지
  - `/api/public/checkout-session`
  - `/api/public/payment/complete`
  - PortOne SDK 호출

### `/`

- 홈 히어로를 “사이트 진단 설명”에서 “결제 직전 멈춤 원인” 중심으로 변경
- 잘못 연결되어 있던 보조 CTA 수정
  - 기존: 상품·요금 보기 → 무료 진단 링크
  - 수정: 상품·요금 보기 → `/plans`

### `/products/veridion/demo`

- 결과 화면에 남아 있던 내부 점수/검증 기준형 문구 제거
- 무료 진단 이후 선택 가능한 결과물 흐름으로 재작성

### `/service`

- 내부 작동 방식 설명을 공개용 서비스 흐름으로 변경
- 내부 점수, 락, 발행 주기, 검증 기준 표현 제거
- 무료 진단, 상세 리포트, FixPack, Auto의 고객용 결과물로 재정리

### 서버 상품 카탈로그

- `Report`, `FixPack`, `Auto` 상품 문구를 구매자 관점으로 재작성
- 가격과 결제 구조는 유지

## 4. 결제 연동 보존 항목

다음 구조는 유지했습니다.

- `data-plan-code`
- `data-price`
- `data-checkout-href`
- `/api/public/payment/config`
- `/api/public/checkout-session`
- `/api/public/payment/complete`
- `window.PortOne?.requestPayment`
- 결제 완료 후 서버 검증 흐름

## 5. 테스트 리뷰

실행 완료:

```bash
npm run phase214:final
```

세부 실행 항목:

```bash
npm run check:syntax
npm run test:phase214
npm run validate:phase214-public-site
npm run check:pages
npm run test:routes
npm run test:all
```

결과:

- 문법 검사: 통과, 250개 소스 확인
- PHASE214 UX/카피 테스트: 통과
- PHASE214 공개 사이트 검증: 통과
- 페이지 무결성 검사: 통과, 34개 라우트 확인
- 라우트 스모크 테스트: 통과, 24개 라우트 확인
- 전체 테스트: 통과, 87/87

## 6. 운영 배포 후 확인 필요

로컬 패키지는 코드·정적 화면·라우팅·테스트 기준 검증입니다. 실제 운영 결제 승인, 정산, 웹훅 수신 여부는 운영 환경에서 직접 확인해야 하며 이 정보는 확인되지 않았습니다.

배포 후 확인 항목:

- `/plans`에서 두 번째 스크린샷 영역이 제거되었는지
- 비교 영역이 표가 아니라 카드형으로 보이는지
- 모바일에서 카드가 한 열로 자연스럽게 정렬되는지
- FixPack 신청 버튼이 결제 가능 상태에서 `/checkout?plan=FixPack`으로 이어지는지
- PortOne 운영 환경값 설정 후 실제 승인과 완료 검증이 되는지
