# Phase177 결과 UI 문구·CTA 게시판 검색 친화 최적화 보고서

## 처리 목적
결과 화면이 처리 방식과 고지 중심으로 보이던 문제를 수정했다. 사용자가 먼저 보고 싶은 것은 “무엇이 확인됐고, 무엇이 부족하며, 무엇을 고치면 되는지”이므로 발견 결과 중심 UI와 세부 요소 중심 게시글 구조로 전환했다.

## 결과 UI 개편
- 상단에 `확인된 요소`, `누락 의심`, `검토 필요`, `자동 초안` KPI를 추가했다.
- `고지` 라벨 중심 표현을 `확인됨`, `누락 의심`, `검토 필요`로 바꿨다.
- 각 발견 항목에 `환불 가능 조건`, `취소 접수 위치`, `수집 목적`, `사업자번호`, `제공 범위` 같은 세부 요소 칩을 추가했다.
- 자동화 설명은 뒤로 내리고, 사용자가 바로 이해할 결과 요약을 앞에 배치했다.

## CTA 게시판 글 개편
- 기존 짧은 안내문을 검색 친화 공개 도움 글 구조로 확장했다.
- 모든 자동 발행 글은 다음 섹션을 포함한다.
  - 왜 이 글을 썼나요?
  - 한눈에 보는 핵심 요약
  - 지금 보이는 문제
  - 고객 입장에서 보면
  - 실제로 확인할 요소
  - 바로 고칠 수 있는 것
  - 문구를 쉽게 바꾸는 방법
  - 검색에 잘 읽히게 정리하는 방법
  - 제목 후보
  - 자주 묻는 질문
  - 다음에 할 일
  - 관련 링크
- 제목, 본문, FAQ, 내부 링크, 세부 요소를 조합해 같은 구조의 반복 발행을 줄였다.
- 검색 노출만을 위한 반복 키워드 삽입, 법률 확정 표현, 성과 보장 표현은 금지했다.

## 개발 인계 파일
- `apps/public/veridion-demo/app.js`
- `apps/public/veridion-demo/app.css`
- `apps/public/board/app.js`
- `server/core/cta-publication.mjs`
- `server/index.mjs`
- `runtime/data/db.seed.json`
- `server/contracts/result-ui-copy-spec.phase177.json`
- `scripts/validate-phase177-result-ui-cta-search.mjs`

## 검증 기준
- 결과 UI에 발견 결과 KPI가 있어야 한다.
- `고지` 중심 카드가 아니라 `확인됨`, `누락 의심`, `검토 필요` 상태를 보여야 한다.
- 게시글 본문은 세부 요소, 체크리스트, 문구 수정 예시, FAQ, 내부 링크를 포함해야 한다.
- 기존 자동 발행 글도 공개 응답과 seed 데이터에서 phase177 구조로 바뀌어야 한다.
