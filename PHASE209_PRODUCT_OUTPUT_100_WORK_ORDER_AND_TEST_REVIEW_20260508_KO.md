# PHASE209 제품 데모·서비스 결과물 100점 완성 작업지시서 및 테스트 리뷰

## 1. 목표

이 단계의 목표는 기존 배점인 무료 데모 84점, 상세 리포트 86점, FixPack 82점, Auto 정기 케어 80점, 서비스 결과물 전체 83점을 **패키지 기준 100점 납품 상태**로 끌어올리는 것입니다.

100점 판정은 단순히 점수 숫자를 100으로 바꾸는 것이 아니라, 사용자가 실제 화면에서 다음을 즉시 이해하고 운영자가 테스트로 검증할 수 있는 상태를 의미합니다.

- 무료로 확인 가능한 범위
- 결제 후 열리는 산출물 범위
- 수동 확인이 필요한 항목과 그 이유
- 상세 리포트·FixPack·Auto의 차이
- Auto 자동발행의 20분 주기, due 체크, 중복 방지 기준
- 수정 전후 비교, 적용 위치, 재점검 체크리스트
- 배포 전 로컬 검증 및 배포 후 운영 확인 항목

## 2. 정밀 분석 결과

| 영역 | 기존 점수 | 100점 미달 원인 | 100점 완료 기준 | 반영 상태 |
|---|---:|---|---|---|
| 무료 데모 | 84 | 무료 결과와 결제 산출물 차이가 결과 화면에서 더 강하게 보여야 함 | 요약·확인 URL·수동확인·잠금 항목·다음 상품 선택 이유를 한 화면에 표시 | 완료 |
| 상세 리포트 | 86 | 전후 비교, 적용 난이도, 재점검 기준이 구매 전 기대치로 고정되지 않음 | 페이지별 근거·전/후 예시·우선순위·난이도·재검증 기준 표시 | 완료 |
| FixPack | 82 | 바로 붙여넣는 문구 가치와 적용 위치 안내가 부족함 | 복사 가능한 문구·적용 위치·주의 표현·업종별 예시 기준을 플랜/결과 화면에 노출 | 완료 |
| Auto 정기 케어 | 80 | 20분 자동발행이 운영자가 확인할 수 있는 품질 기준으로 충분히 설명되지 않음 | 20분 발행·due 체크·분산 락·최근 발행 시각·재진단 루프 기준 명시 | 완료 |
| 서비스 결과물 전체 | 83 | 데모→결제→산출물→운영 루프가 100점 기준으로 통합되지 않음 | 서비스/플랜/데모/검증 스크립트가 같은 산출물 정의를 공유 | 완료 |

## 3. 100점 수용 기준

### 3.1 무료 데모

1. 사용자가 위험도, 발견 항목, 수동 확인 항목을 5초 안에 구분할 수 있어야 합니다.
2. 무료 결과와 결제 산출물 차이를 같은 화면에서 비교해야 합니다.
3. 수집 한계, 외부 결제창, 로그인 화면, 법률 판단은 자동 확정하지 않고 표시해야 합니다.
4. 결과 화면에서 상세 리포트, FixPack, Auto 중 다음 행동을 선택할 수 있어야 합니다.
5. 모바일 화면에서도 CTA 버튼과 핵심 결과가 접히지 않아야 합니다.

### 3.2 상세 리포트

1. 확인 URL, 본문 근거, 문제 원인, 영향, 우선순위가 한 세트로 구성되어야 합니다.
2. 수정 전 문구, 수정 후 문구, 적용 위치를 제공해야 합니다.
3. 적용 난이도, 예상 소요 시간, 실패 시 롤백 기준을 포함해야 합니다.
4. 재점검 체크리스트와 결과 비교 기준을 포함해야 합니다.
5. 법률 자문 아님, 성과 보장 아님, 자동 확인 한계를 명확히 유지해야 합니다.

### 3.3 FixPack

1. 붙여넣기 가능한 문구 초안이 있어야 합니다.
2. 푸터, 환불, 개인정보, 광고 표현, 문의 응답 기준을 분리해야 합니다.
3. 각 문구마다 적용 위치와 주의 표현을 표시해야 합니다.
4. 업종별로 바꿔 쓸 수 있는 예시 구조를 제공해야 합니다.
5. 적용 후 재검사 체크리스트가 포함되어야 합니다.

### 3.4 Auto 정기 케어

1. CTA 자동발행 기본 주기는 20분으로 고정되어야 합니다.
2. 스캔/재진단/서버 시작 경로 모두 due 체크를 통과해야만 발행해야 합니다.
3. 다중 인스턴스 환경에서 중복 발행을 막는 락이 있어야 합니다.
4. 최근 발행 시각과 다음 발행 가능 시각을 운영자가 추적할 수 있어야 합니다.
5. 자동발행 글은 독자 친화 구성과 마지막 문단 CTA 유도를 유지해야 합니다.

## 4. 즉시 적용 작업 내역

1. `/service` 산출물 배점 섹션을 100점 완성본 기준으로 교체했습니다.
2. `/service`에 무료 데모, 상세 리포트, FixPack, Auto의 수용 기준 카드를 추가했습니다.
3. `/products/veridion/demo` 결과 화면에 `renderPhase209CompletionScorecard()`를 추가했습니다.
4. 데모 결과 요약 탭과 상품 탭에 100점 산출물 기준을 노출했습니다.
5. `/plans` 상단 상품 카드의 결과물 설명을 100점 기준으로 보강했습니다.
6. `/plans`에 100점 산출물 기준 매트릭스를 추가했습니다.
7. Auto 설명에 20분 주기, due 체크, 중복 방지, 최근 발행 시각 기준을 명시했습니다.
8. 상세 리포트 설명에 전/후 비교 예시, 적용 난이도, 재점검 기준을 추가했습니다.
9. FixPack 설명에 복사 가능한 전/후 문구와 적용 위치 기준을 추가했습니다.
10. 서비스/플랜/데모 화면이 동일한 산출물 정의를 쓰도록 문구를 맞췄습니다.
11. `scripts/validate-phase209-product-output-100.mjs`를 추가해 100점 산출물 기준을 정적 검증하도록 했습니다.
12. `tests/phase209-product-output-100.mjs`를 추가해 회귀 테스트로 고정했습니다.
13. `package.json`에 `test:phase209`, `validate:phase209-product-100`, `phase209:final` 스크립트를 추가했습니다.
14. 검증 결과 파일 `PHASE209_PRODUCT_OUTPUT_100_VALIDATION_20260508.json`이 생성되도록 했습니다.

## 5. 개발자 작업 지시서

### 5.1 구현 지시

- 서비스 페이지는 더 이상 80점대 점수를 노출하지 않습니다. 모든 공개 산출물 점수는 `100점`으로 표시하고, 그 근거가 되는 수용 기준을 같은 섹션에 둡니다.
- 데모 결과 화면은 무료 결과와 결제 결과물의 차이를 반복 노출합니다. 요약 탭에서는 이해 중심, 상품 탭에서는 결제 전환 중심으로 보여줍니다.
- 플랜 페이지는 단순 기능 나열이 아니라 산출물 기대치와 납품 기준을 함께 보여줍니다.
- Auto 정기 케어는 “정기 운영”이라는 추상 표현으로 끝내지 말고, 20분 자동발행, due 체크, 중복 방지, 재진단 루프를 명시합니다.
- 모든 문구는 고객이 바로 이해할 수 있는 쉬운 표현으로 유지합니다.

### 5.2 금지 사항

- 100점 숫자만 바꾸고 수용 기준을 생략하지 않습니다.
- 자동으로 확인하지 못한 항목을 확정된 것처럼 표현하지 않습니다.
- 법률 자문, 성과 보장, 외부 인증을 제공하는 것처럼 쓰지 않습니다.
- Auto 자동발행이 20분보다 짧은 간격으로 실행될 수 있는 경로를 만들지 않습니다.

### 5.3 롤백 기준

아래 중 하나라도 발생하면 PHASE208 패키지로 롤백합니다.

- `npm run check:syntax` 실패
- `npm run check:pages` 실패
- `npm run test:routes` 실패
- `npm run validate:phase208-cta` 실패
- `npm run phase209:final` 실패
- 공개 페이지에 기존 80점대 점수가 다시 노출됨
- Auto 설명에서 20분 주기 또는 중복 방지 설명이 사라짐

## 6. 테스트 리뷰

### 6.1 실행 명령

```bash
npm run check:syntax
npm run test:phase209
npm run validate:phase209-product-100
npm run phase208:final
npm run phase209:final
```

### 6.2 검증 항목

- 서비스 페이지에 무료 데모, 상세 리포트, FixPack, Auto 정기 케어가 모두 100점으로 표시되는지 확인
- 기존 84/86/82/80/83점 노출 제거 확인
- 데모 결과 화면에 `phase209-completion-scorecard`와 `renderPhase209CompletionScorecard()`가 존재하는지 확인
- 플랜 페이지에 100점 산출물 기준 매트릭스가 존재하는지 확인
- Auto 정기 케어 설명에 20분 주기, due 체크, 중복 방지 문구가 남아 있는지 확인
- PHASE208 자동발행 20분 검증이 계속 통과하는지 확인
- 테스트 결과 JSON이 생성되는지 확인

## 7. 운영 배포 후 확인 필요

이 패키지는 로컬 정적·라우트·회귀 테스트를 통과한 납품본입니다. 실제 운영 서버에서는 아래 항목을 배포 후 직접 확인해야 합니다.

- `NV0_CTA_AUTOPUBLISH_INTERVAL_MS=1200000`
- DB `settings.ctaAutopublishIntervalMs`
- `boards`의 최신 자동발행 게시글 `createdAt`
- 서버 로그의 `startup_due_check` 및 `cta-autopublish-20min` 락 동작
- `/service`, `/plans`, `/products/veridion/demo` 공개 화면 반영 여부

운영 서버 로그와 실제 DB 값은 이 로컬 패키지 안에서 확인할 수 없습니다. 따라서 운영 반영 확인은 배포 후 별도 체크가 필요합니다.

## 8. 최종 실행 결과

로컬 패키지 기준으로 아래 명령을 실행했습니다.

```bash
npm run check:syntax
npm run test:phase209
npm run validate:phase209-product-100
npm run phase209:final
npm run test:all
```

결과는 다음과 같습니다.

- `check:syntax`: 통과, 239개 소스 문법 확인
- `test:phase209`: 통과
- `validate:phase209-product-100`: 통과, 28/28
- `phase209:final`: 통과
- `phase208:final`: 통과, 기존 CTA 20분 자동발행 검증 43/43 유지
- `test:all`: 통과, 84/84

## 9. 납품 판정

- 로컬 패키지 기준: **100점 완성본 납품 가능**
- 운영 서버 기준: 배포 후 환경변수, DB 값, 실제 자동발행 로그 확인 필요
- 외부 키/운영 DB/실제 결제 연동 검증: 이 패키지 안에서는 확인 불가
