# PHASE230 전역 시인성·전환 구조 검수 및 에이전틱 코딩 작업지시서

## 목적
nv0.kr 공개 화면의 시인성 저하, 가격 노출 불일치, 무료 데모의 구매 전환 메시지 약화, 푸터/카드/CTA 밀집 문제를 전역에서 정리한다. 무료 데모는 문제 영역·영향 요소·갯수·위기도를 선명하게 보여주고, 유료 서비스 구매로 이어지는 시각 흐름을 강화한다.

## 라이브 확인에서 발견한 주요 현상
- `/plans` 공개 화면에서 이전 가격 69,000원, 99,000원, 299,000원이 노출되는 현상이 확인됨.
- 홈과 데모의 안내는 기능 설명은 있으나 위기도/문제 갯수/구매 이유가 첫 화면에서 충분히 강하지 않음.
- 푸터 사업자 고지와 주요 CTA가 한 덩어리처럼 보이며, 모바일/좁은 화면에서 메뉴와 버튼이 밀집될 가능성이 큼.
- 기존 CSS 레이어가 여러 Phase로 누적되어 페이지별 시각 권한이 분산됨.

## 진단 수량
총 46개 보완 대상을 아래처럼 분류했다.

| 영역 | 수량 | 조치 |
|---|---:|---|
| 라이브 `/plans` 가격 시인성/불일치 | 6 | 이전 가격 노출 제거, 39,000/79,000/149,000원 구조 고정 |
| 전역 CSS 권한층 누락 | 17 | 모든 공개 페이지에 phase230 최종 시각 보정 CSS 로드 |
| 내비게이션·CTA 밀집 | 3 | 탑바, CTA 높이, 반응형 메뉴 그리드 표준화 |
| 히어로·카드 위계 약화 | 5 | 제목 크기, 행간, 카드 대비, 경계선 강화 |
| 푸터/사업자 고지 가독성 | 4 | inline 나열을 2열 그리드와 링크 칩 구조로 정리 |
| 데모 전환 메시지 약화 | 4 | 위기도 점수, 문제 영역, 요소와 갯수, 구매 연결 문구 강화 |
| 폼/입력창 시인성 | 3 | 입력 높이, 배경 대비, placeholder, focus ring 보강 |
| 모바일 가독성 위험 | 4 | 1열 카드, full-width CTA, 메뉴 collapse 규칙 적용 |

## 적용 작업
1. `shared/phase230-visual-clarity-conversion.css` 추가
   - 전역 최종 시각 권한층으로 모든 공개 페이지의 색상, 대비, 패널, CTA, 모바일 규칙을 통제한다.
2. 17개 공개 페이지 HTML에 Phase230 CSS 링크 추가
   - `phase224-readable-marketing.css` 뒤에 로드하여 기존 스타일보다 우선 적용한다.
3. 무료 데모 첫 화면 문구 수정
   - “구매를 막는 위기도와 문제 갯수”를 첫 헤드라인에 배치한다.
   - 무료 진단 계약을 `위기도 점수 / 문제 영역 / 요소와 갯수 / 구매 연결`로 명확히 표현한다.
4. `/plans` 상태 문구 수정
   - 중립적 안내인 “상품 정보를 준비했습니다”를 제거하고 FixPack 79,000원 추천 문구로 대체한다.
5. 검증 스크립트 추가
   - `tests/phase230-visual-clarity-conversion.mjs`
   - `scripts/validate-phase230-visual-clarity-conversion.mjs`
6. 회귀 테스트 보정
   - `tests/e2e.mjs`의 버전 허용 범위를 phase230 이후까지 확장한다.

## 완료 기준
- 모든 공개 페이지가 Phase230 최종 CSS를 로드한다.
- Phase230 CSS가 Phase224 이후에 로드된다.
- `/plans`에서 이전 가격 문자열이 사라진다.
- 데모 첫 화면에 위기도 점수, 문제 영역, 요소와 갯수, 구매 연결이 보인다.
- 문법, 라우트, E2E, 링크 검사가 모두 통과한다.

## 검증 결과
- `npm run phase230:final`: OK
- `npm run test:all`: OK / 84 passed / 0 failed
- `npm run check:links -- --summary`: OK / 537 checked / 0 errors

## 운영 반영 주의
이 패키지는 로컬 소스 기준으로 완성되었다. 실제 nv0.kr에 반영하려면 운영 서버 배포 후 Cloudflare/Coolify 캐시를 비워야 한다. 라이브 `/plans`가 이전 가격을 계속 보이면 배포본 또는 CDN 캐시가 과거 파일을 제공하는 상태다.
