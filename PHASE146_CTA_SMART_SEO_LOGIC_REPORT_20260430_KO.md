# PHASE146 CTA 스마트 SEO 발행 로직 개선 보고서

## 목적

CTA 게시판 자동 발행 글이 고정 템플릿처럼 반복되는 문제를 줄이고, 검색 의도·업종·고객 단계·주요 진단 항목을 반영해 매번 다른 구조의 포스팅이 생성되도록 개선했습니다.

## 핵심 변경

1. `server/core/cta-publication.mjs`
   - 기존 12개 고정 주제 순환을 24개 SEO 주제팩으로 확장했습니다.
   - 검색 의도(intent), 고객 퍼널 단계(funnel), 페르소나, 핵심 키워드, 보조 키워드, 메타 설명 후보를 생성 결과에 포함했습니다.
   - 제목 후보, 도입, 문제 제기, 해결 과정, 신뢰 근거, FAQ, 자연스러운 CTA, 태그 섹션을 유지하면서 본문 조합을 다변화했습니다.
   - URL 전체가 제목에 노출되지 않도록 도메인 표시로 정리했습니다.
   - 금지 표현(100% 보장, 무조건 해결, 법률 위반 단정)을 사용하지 않도록 유지했습니다.

2. `server/index.mjs`
   - 중복 회피 탐색 범위를 12회에서 48회로 확대했습니다.
   - 발행 데이터에 `seoQualityStandard`, `searchIntent`, `funnelStage`, `primaryKeyword`, `secondaryKeywords`, `metaDescription` 메타데이터를 저장하도록 했습니다.
   - 기존 P143/P144/P145 배포 안정화 패치는 유지했습니다.

3. `scripts/validate-phase146-cta-smart-seo.mjs`
   - 30개 샘플 생성 기준으로 제목 중복, 본문 fingerprint 중복, CTA 타입 다양성, 검색 의도 다양성, 필수 섹션, SEO 메타데이터를 검증합니다.

## 검증 결과

- 생성 샘플: 30개
- 제목 고유성: 30/30
- 본문 fingerprint 고유성: 30/30
- CTA 타입 다양성: 24개
- 검색 의도 다양성: 12개
- 필수 섹션 누락: 0
- SEO 메타데이터 누락: 0
- 금지 보장/법률 단정 표현: 없음

## 운영 참고

이 패치는 앞으로 새로 자동 발행되는 CTA 글의 품질과 다양성을 개선합니다. 이미 운영 DB에 저장된 기존 중복 게시글은 자동 삭제하지 않습니다. 기존 글 정리는 운영 정책에 따라 별도 백업 후 선별 삭제 또는 새 글 재발행으로 처리하는 것이 안전합니다.
