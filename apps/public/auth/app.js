const loginState = document.getElementById('loginState');
const registerState = document.getElementById('registerState');
const resetRequestState = document.getElementById('resetRequestState');
const resetState = document.getElementById('resetState');
function targetPortal(){ const url = new URL(location.href); return url.searchParams.get('next') || '/portal'; }
async function postJson(path, payload){ const res = await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)}); const data = await res.json().catch(()=>({})); if(!res.ok) throw new Error(data.error || '요청을 처리하지 못했습니다.'); return data; }
document.getElementById('loginForm')?.addEventListener('submit', async (event)=>{ event.preventDefault(); loginState.textContent='로그인 중입니다...'; try{ await postJson('/api/public/auth/login',{email:loginEmail.value,password:loginPassword.value}); location.href=targetPortal(); }catch(error){ loginState.textContent=error.message; }});
document.getElementById('registerForm')?.addEventListener('submit', async (event)=>{ event.preventDefault(); registerState.textContent='계정을 생성하는 중입니다...'; try{ await postJson('/api/public/auth/register',{email:registerEmail.value,password:registerPassword.value,privacyConsent:privacyConsent.checked}); location.href=targetPortal(); }catch(error){ registerState.textContent=error.message; }});
document.getElementById('resetRequestForm')?.addEventListener('submit', async (event)=>{ event.preventDefault(); resetRequestState.textContent='재설정 안내를 준비하는 중입니다...'; try{ const data=await postJson('/api/public/auth/request-password-reset',{email:resetEmail.value}); resetRequestState.textContent=data.message || '가입된 이메일이면 재설정 안내가 발송됩니다.'; }catch(error){ resetRequestState.textContent=error.message; }});
document.getElementById('resetForm')?.addEventListener('submit', async (event)=>{ event.preventDefault(); resetState.textContent='비밀번호를 변경하는 중입니다...'; try{ const data=await postJson('/api/public/auth/reset-password',{email:resetConfirmEmail.value,token:resetToken.value,password:resetPassword.value}); resetState.textContent=data.message || '비밀번호가 변경되었습니다.'; }catch(error){ resetState.textContent=error.message; }});
(()=>{ const url=new URL(location.href); const token=url.searchParams.get('resetToken') || ''; const email=url.searchParams.get('email') || ''; if(token) resetToken.value=token; if(email){ resetConfirmEmail.value=email; resetEmail.value=email; loginEmail.value=email; } })();
(async()=>{ try{ const res = await fetch('/api/public/auth/session'); const data=await res.json(); if(data.authenticated) loginState.textContent=`${data.customer.email} 계정으로 로그인되어 있습니다.`; }catch{} })();
