# PHASE205 Live UX Closeout Report — 2026-05-05

## 목적
`nv0.kr` 실서비스 화면에서 확인된 깨짐, 겹침, 시인성 저하, 고정 예시 점수, 데모 실패, 좌측 메뉴 비동작, 새 사이트 등록 위치 문제를 기준으로 패키지 소스를 재정리했습니다.

## 실서비스에서 확인한 문제
- 홈 화면에 `신뢰도 점수 72/100`, `샘플 진단 결과`, 고정 숫자 지표가 남아 있어 실제 진단 전 화면처럼 보이지 않았습니다.
- 무료 진단 화면에도 `72`와 `무료 진단 결과 예시`가 남아 있어 사용자가 입력하기 전부터 결과가 있는 것처럼 보였습니다.
- 콘텐츠 보드는 필터/주제 영역이 메뉴처럼 보이지만 유효한 상호작용과 fallback 품질이 부족했습니다.
- 내 사이트 화면은 `새 사이트 등록`이 하단 흐름에 묻히고 페이지 목적이 `다음 조치`에 치우쳐 있었습니다.
- 푸터에는 배포본에서 `replace-with-number`가 노출된 흔적이 있어 통신판매업 신고번호 placeholder 방어가 필요했습니다.

## 수정 범위
1. 홈
   - 고정 `72점` 예시와 정적 수치성 결과 패널 제거.
   - `검사 후 표시되는 항목` 구조로 변경.
   - 결과 전용 안내만 남겨 실제 검사 전/후 상태를 분리.

2. 무료 진단
   - 기본 화면의 고정 점수 예시 제거 유지.
   - API 502, timeout, 서버 지연 시 `ERR` 단독 화면이 아니라 `client_safe_fallback` 결과를 표시하도록 보강.
   - 결과 패널 과도한 빈 여백 축소.

3. 콘텐츠 보드
   - 좌측 필터와 주제를 전부 실제 `<button>` 제어로 유지.
   - `공지`, `사례` 초기 카운트의 `-` 제거.
   - `/api/public/board` 실패 시에도 기본 게시글 3개, 최근 활동, 통계가 유지되도록 `FALLBACK_POSTS` 추가.

4. 내 사이트
   - 페이지 헤드라인을 `새 사이트 등록과 다음 조치`로 변경.
   - 새 사이트 등록 섹션을 다음 조치보다 위에 유지.
   - 포털 내부 고객지원 링크를 `/business-info`로 통일.

5. 플랜 비교
   - Pro `추천` 배지와 `1회 점검` 칩이 겹치지 않도록 static 위치 유지.

6. 전역 디자인 시스템
   - 1180~1365px 구간에서 카드, 버튼, 필터, 우측바가 과밀해지지 않도록 Phase205 guardrail 추가.
   - `overflow-x:hidden`, `min-width:0`, `overflow-wrap:anywhere` 보강.
   - 1280px 이하 콘텐츠 보드 우측바를 아래로 내려 본문 폭 확보.
   - 데모 결과 패널 최소 높이 축소 및 빈 결과 영역 정렬.

## 재발 방지 검증
추가 스크립트:

```bash
npm run validate:phase205
npm run phase205:final
```

검증 결과:
- `check:syntax`: 231개 소스 통과
- `check:pages`: 34개 라우트 통과
- `test:routes`: 24개 라우트 통과
- `validate:phase204`: 11개 통과
- `validate:phase205`: 12개 통과
- `check:links -- --summary`: 456개 링크 통과

## 배포 주의
실서비스 `nv0.kr`는 이전 배포본이 보여서 동일 문제가 계속 보일 수 있습니다. 이 패키지 반영 후 CDN/Cloudflare 캐시와 브라우저 캐시를 같이 purge해야 합니다.
